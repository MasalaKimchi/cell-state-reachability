# ==========================================================================
# cell-state-reachability — pip requirements
# ==========================================================================
# The CORE method (reachability.py) depends only on numpy + scipy. Those two
# plus pytest are all that is needed to run the test suite and reproduce the
# headline verdict. The remaining packages are for the analysis notebooks and
# figures. Versions marked "==" are the exact versions this work was validated
# against (Python 3.11); the notebook/EDA extras use ">=" floors.
#
#   pip install -r requirements.txt
#
# For a minimal core-only install (method + tests, no notebooks):
#   pip install "numpy==2.4.6" "scipy==1.17.1" pytest
# --------------------------------------------------------------------------

# --- core method (reachability.py) : validated exact pins ---
numpy==2.4.6
scipy==1.17.1

# --- analysis / figures (validated exact pins) ---
pandas==2.3.3
matplotlib==3.11.0
anndata==0.12.19
h5py==3.16.0

# --- test runner ---
pytest>=7.4

# --- notebook execution + EDA (version floors; not pinned in the core env) ---
scanpy>=1.10
nbformat>=5.9
nbconvert>=7.10
ipykernel>=6.25
scikit-learn>=1.3
